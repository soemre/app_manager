name: app_manager
description: A lightweight Flutter package that simplifies the customization of Flutter apps.
version: 0.1.1
repository: https://github.com/emresoysuren/app_manager

environment:
  sdk: '>=3.1.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
